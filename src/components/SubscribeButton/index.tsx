import styles from './styles.module.scss'

export function SubscrubeButton() {
  return (
    <button
      type="button"
      className={styles.subscribeButton}
    >
      Subscribe now
    </button>
  )
}
